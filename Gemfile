source "https://rubygems.org"

# This gem manages the specific version of Jekyll used with GitHub Pages.
gem "github-pages", group: :jekyll_plugins

# If you are on Windows, include this dependency.
gem "wdm", "~> 0.1.0" if Gem.win_platform?

# If you have any plugins, put them here!
group :jekyll_plugins do
  gem "jekyll-feed"        # Add any required version if needed
  gem "jekyll-sitemap"     # Add any required version if needed
  gem "hawkins"            # Check if any version constraints are needed
  # Uncomment below if you want to use it
  # gem "jekyll-last-modified-at"
end